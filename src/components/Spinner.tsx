const Spinner = () => {
  return (
    <div className="fixed top-0 left-0 w-full h-full bg-gray-800 bg-opacity-20 flex justify-center items-center z-50">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
